import React from 'react';
import GlobalStyles from '../../GlobalStyles';

interface CoreProps {}
const Core: React.FC<CoreProps> = ({}) => {
  return (
    <>
      <GlobalStyles />
    </>
  );
};

export default Core;
